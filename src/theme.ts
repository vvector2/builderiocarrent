import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3563E9",
    },
    secondary: {
      main: "#1A202C",
    },
  },
  typography: {
    fontFamily: "Plus Jakarta Sans",
  },
});
export { theme };
